<section class="hero" aria-labelledby="home-title">
  <slot />
</section>

<style>
  .hero {
    display: grid;
    gap: 30px;
    justify-items: center;
    text-align: center;
  }

  .hero :global(h1) {
    margin: 0;
    font-size: clamp(2.8rem, 8vw, 5.7rem);
    line-height: 0.95;
    font-weight: 780;
    letter-spacing: 0;
  }

  .hero :global(.hero__logo) {
    width: min(310px, 68vw);
    height: auto;
    filter: drop-shadow(0 26px 52px var(--glow-accent));
  }

  .hero :global(.hero__lead) {
    max-width: 700px;
    margin: 0;
    color: var(--text-soft);
    font-size: clamp(1.05rem, 2.5vw, 1.28rem);
    line-height: 1.7;
  }
</style>
